<div class="header">
  <h2><%= @content.section.titleize %></h2>
</div>

<div class="section content">
  <label><%= @content.category.titleize %>
  <%= @content.id %></label>
  <label><%= @content.key %></label>
  <span><%= @content.text %></span>
  <% if @content.category == "question" %>
    <% @content.relationships.each_with_index do | rel, index | %>
      <label><%= t(".answer", number: index+1 ) %></label>
      <span><%= rel %></span>
    <% end %>
  <% else %>
    <label><%= t(".score") %></label>
    <span> <%= @content.score %></span>
    <label><%= t(".option") %></label>
    <span> <!-- @content.relationships[0].to_content --></span>
  <% end %>
</div>

<div class="actions">
  <%= link_to t("edit"), edit_content_path(@content), class: "left" %>
  <%= link_to t("back"), contents_path, class: "right" %>
</div>
